console.log in html log in the browser console, bc it's executed by the browser
console.log in server.ts is executed by the node.js runtime in docker container

because bundle.js is served, on buttonClick doesn't request anything
